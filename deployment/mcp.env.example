# Digital Samba MCP Server - Application Configuration
#
# Copy this file to mcp.env and fill in the values
#
# Note: In HTTP mode, authentication is handled via:
# 1. OAuth (recommended) - User authenticates via DS Passport
# 2. Direct API key - Client provides API key in Authorization header

# Optional: Override API URL (defaults to https://api.digitalsamba.com/api/v1)
# DIGITAL_SAMBA_API_URL=https://api.digitalsamba.com/api/v1

# Optional: Log level (debug, info, warn, error)
# LOG_LEVEL=info

# ============================================================================
# OAuth Configuration (for DS Passport integration)
# ============================================================================
# Create an OAuth client in DS Passport:
#   php artisan passport:client --name="MCP Server" --redirect_uri="<callback_url>"

# OAuth Client Credentials (from passport:client command)
OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=

# OAuth Endpoints (adjust for your environment)
# Note: OAuth sessions use /oauth-api/v1/* instead of /api/v1/* for API calls
#       This is handled automatically - no need for developer_key retrieval

# Production:
# OAUTH_AUTHORIZE_URL=https://dashboard.digitalsamba.com/oauth
# OAUTH_TOKEN_URL=https://api.digitalsamba.com/oauth/token
# OAUTH_API_URL=https://api.digitalsamba.com/oauth-api/v1
# OAUTH_REDIRECT_URI=https://mcp.digitalsamba.com/oauth/callback
# OAUTH_ISSUER=https://mcp.digitalsamba.com

# Development:
# OAUTH_AUTHORIZE_URL=https://dashboard.dev.monza.digitalsamba.com/oauth
# OAUTH_TOKEN_URL=https://api-dev.dev.monza.digitalsamba.com/oauth/token
# OAUTH_API_URL=https://api-dev.dev.monza.digitalsamba.com/oauth-api/v1
# OAUTH_REDIRECT_URI=https://mcp-dev.digitalsamba.com/oauth/callback
# OAUTH_ISSUER=https://mcp-dev.digitalsamba.com
